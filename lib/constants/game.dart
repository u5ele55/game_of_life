import 'package:flutter/painting.dart';

const int fieldHeight = 120;
const int fieldWidth = 80;

const double cellSize = 64;

const Duration updateDuration = Duration(milliseconds: 250);

const Color cellColor = Color.fromARGB(255, 119, 16, 94);
const Color lineColor = Color(0xff995588);
